# /check — 전체 품질 검사

빌드 → 테스트를 순서대로 실행하고 결과를 요약해줘.

## 실행 순서

1. `cd backend && ./gradlew build` 실행
2. 빌드 실패 시 에러 원인 분석하고 중단
3. 테스트 결과 요약

## 결과 보고 형식

```
✅ 빌드: 성공 / ❌ 빌드: 실패 (원인)
✅ 테스트: N개 통과 / ❌ 테스트: N개 실패 (실패 목록)
```

실패 항목이 있으면 수정 방법을 구체적으로 제안해줘.
